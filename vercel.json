{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --prefix itm && npm ci --prefix nc && npm ci --prefix www",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
