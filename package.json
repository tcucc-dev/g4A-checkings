{
  "name": "tcu-webinsight-reports",
  "version": "1.0.0",
  "private": true,
  "description": "TCU department web insight reports (ITM + NC), deployed via Vercel monorepo (rootDirectory per project)",
  "scripts": {
    "build:itm": "cd itm && npm install && npm run build",
    "build:nc": "cd nc && npm install && npm run build",
    "dev:itm": "cd itm && npm run dev",
    "dev:nc": "cd nc && npm run dev"
  }
}
