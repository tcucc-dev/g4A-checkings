{
  "name": "tcu-webinsight-reports",
  "version": "1.0.0",
  "private": true,
  "description": "TCU department web insight reports (ITM + NC), deployable as a single Vercel project at the repository root or as separate Vite projects per department.",
  "scripts": {
    "build": "node scripts/build-vercel.mjs",
    "build:itm": "npm run build --prefix itm",
    "build:nc": "npm run build --prefix nc",
    "dev:itm": "npm run dev --prefix itm",
    "dev:nc": "npm run dev --prefix nc",
    "preview:itm": "npm run preview --prefix itm",
    "preview:nc": "npm run preview --prefix nc"
  }
}
